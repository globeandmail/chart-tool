setup:
  config:
    NODE_ENV: production
    MAIL_URL: smtp://user:password@mailhost:port/
    DOCKER: 'TRUE'
    PORT: 3000
    ROOT_URL: http://chart-tool-demo.herokuapp.com
  addons:
  - plan: mongolab:sandbox
    as: MONGODB
build:
  docker:
    web: docker/prod.Dockerfile
release:
  image: web
  command:
    - ./deployment-tasks.sh
run:
  web: /docker/entrypoint.sh node main.js
